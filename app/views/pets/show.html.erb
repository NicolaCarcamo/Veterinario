<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @pet.name %>
</p>

<p>
  <strong>Race:</strong>
  <%= @pet.race %>
</p>

<p>
  <strong>Birthdate:</strong>
  <%= @pet.birthdate %>
</p>

<%= link_to 'Pet history', client_pet_pet_histories_path(@client, @pet) %>

<table>
  <thead>
    <tr>
      <th>Peso promedio</th>
      <th>Altura promedio</th>
      <th>Peso Máximo</th>
      <th>Altura Máxima</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td><%= @pet.avg_weight %> kg</td>
      <td><%= @pet.avg_height %> cm</td>
      <td><%= @pet.max_weight %> kg</td>
      <td><%= @pet.max_height%> cm</td>
    </tr>
  </tbody>
</table>

<%= link_to 'Edit', edit_client_pet_path(@client, @pet) %> |
<%= link_to 'Back', client_pets_path(@client)  %>
